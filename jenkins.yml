---
 - hosts: all
   become: yes
   gather_facts: no
   tasks: 
    - name: installing java
      apt:
        name: openjdk-8-jre
        state: present
    - name: installing maven
      apt:
        name: "{{item}}"
        state: latest
        update_cache: yes
      with_items:
          - maven  